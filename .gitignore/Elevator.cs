namespace Final_Exam_Code
{
    class Elevator
    {
        private double MaxWeight;
        private Occupants = Passenger array[];

        public void(int maxOccupants + double maxWeight);
        this.maxWeight = maxWeight()
      }
    {
        Occupant = new Passenger;
        Passenger p1 = new Occupant;
    public Occupant(Passenger Passenger, int index);

    public void();
        Elevator.getCurrentWeight;
        Elevator.isOverMaxCapacity;
    }
}
